name: Makefile CI

on:
  push:
    branches: [ "android15-lineage22-mod" ]
  pull_request:
    branches: [ "android15-lineage22-mod" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
